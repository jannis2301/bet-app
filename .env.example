MONGODB_URI=
JWT_SECRET=
JWT_LIFETIME=1d
PORT=5050
NODE_ENV=development

# optional — outbound mail for password-reset links and matchday reminders
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
